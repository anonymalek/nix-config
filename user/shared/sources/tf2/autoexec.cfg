exec gfx
exec binds
exec settings
exec network

alias dk "unbind q; unbind e; bind q +left; bind e +right"

alias callmedic "voicemenu 0 0"
alias udk "unbind q; unbind e; bind q lastinv; bind e callmedic"
